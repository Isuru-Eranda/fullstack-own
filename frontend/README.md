# Frontend

This folder contains the frontend (Vite + React) from the fullstack project.

Quick start
- Install dependencies: `npm install`
- Run dev server: `npm run dev`

Notes
- Copy `.env.example` to `.env` and set environment variables if needed.
