# Backend

This folder contains the Node/Express backend from the fullstack project.

Quick start
- Install dependencies: `npm install`
- Run server: `npm run dev` or `npm start`

Notes
- Copy `.env.example` to `.env` and set database and other environment variables.
